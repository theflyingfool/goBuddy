# Python virtual environments & bytecode
.venv/
__pycache__/
*.pyc
*.pyo

# IDE & OS files
.DS_Store
.idea/
.vscode/

# Large temporary build databases
*.duckdb.wal

# Regenerated export artifacts -- never meant to be committed
# (output/source_coverage_report.md IS committed by convention)
output/parquet/

# Master DB build output -- too large to push to GitHub (grows past the
# 100MB per-blob hard limit / free LFS quota as more sources are added).
# Not committed while a scalable storage/distribution approach is worked out.
output/GoRefs_Master.duckdb

# Personal, unverified investigation scratchpad -- local-only by design (2026-08-02)
POSSIBLE_ISSUES.md

